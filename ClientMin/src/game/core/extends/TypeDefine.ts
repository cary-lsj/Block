/**
 * 类型定义
 * @author cary
 */
namespace game {
    export type Point = { x: number, y: number };

    export type EntityTweenObj = { x: number, y: number, bodyY: number, alpha: number };

    export type Position = { x?: number, y?: number, left?: number, right?: number, top?: number, bottom?: number, horizontalCenter?: number, verticalCenter?: number };

}
