namespace game {
    /**
     * 模块层
     * @author wizardc
     */
    export const enum ModuleLayer {
        /** 场景层 */
        sceneLayer,
        /** 天气层 */
        weatherLayer,
        /** 底部层 */
        bottomLayer,
        /** 底部 UI 层 */
        bottomUILayer,
        /** 全屏窗口层 */
        fullWindowLayer,
        /** 主界面 UI 层 */
        mainUILayer,
        /** 对话窗口层 */
        dialogLayer,
        /** 弹出层 */
        popLayer,
        /** 警报框层 */
        alertLayer,
        /** 引导层 */
        guideLayer,
        /** 消息层 */
        messageLayer,
        /** 最顶层 */
        topLayer,
    }
}
