namespace game {
    /**
     * 关卡 事件
     */
    export const enum PortEvent {
        //点击按钮
        click,
         /** 点击开始 */
        clickStart,
        /** 点击返回 */
        clickBack,
        /** 播放动画 */
        playOneStar,
        /** 返回到menu */
        clickExit,
    }
}